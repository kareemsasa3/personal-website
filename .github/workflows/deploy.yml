name: CD - Deploy to Production

on:
  workflow_run:
    workflows: ["Build & Publish Images"]
    types:
      - completed

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' && (github.event.workflow_run.head_branch == 'main' || github.event.workflow_run.head_branch == 'master') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    env:
      PROD_DOMAIN: ${{ secrets.PROD_DOMAIN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Deploy via composite action
        uses: ./.github/actions/ssh-deploy
        with:
          host: ${{ secrets.PROD_HOST }}
          user: ${{ secrets.PROD_USER }}
          port: ${{ secrets.PROD_PORT }}
          ssh_key: ${{ secrets.PROD_SSH_KEY }}
          ghcr_username: ${{ github.actor }}
          ghcr_token: ${{ github.token }}
          repository: ${{ github.repository }}
          sha: ${{ github.event.workflow_run.head_sha }}

      - name: Verify production health
        if: ${{ env.PROD_DOMAIN != '' }}
        env:
          DOMAIN: ${{ env.PROD_DOMAIN }}
          ORIGIN_IP: ${{ secrets.PROD_ORIGIN_IP }}
        run: |
          set -e
          echo "Waiting for services at https://$DOMAIN ..."
          for i in {1..60}; do
            ok=0
            if [ -n "$ORIGIN_IP" ]; then
              curl -fsS -m 10 --resolve "$DOMAIN:443:$ORIGIN_IP" "https://$DOMAIN/health" >/dev/null && ok=$((ok+1)) || true
              # Grafana proxied under subpath
              curl -fsS -m 10 --resolve "$DOMAIN:443:$ORIGIN_IP" "https://$DOMAIN/monitoring/grafana/api/health" >/dev/null && ok=$((ok+1)) || true
            else
              curl -fsS -m 5 "https://$DOMAIN/health" >/dev/null && ok=$((ok+1)) || true
              curl -fsS -m 5 "https://$DOMAIN/monitoring/grafana/api/health" >/dev/null && ok=$((ok+1)) || true
            fi
            if [ "$ok" -eq 2 ]; then
              echo "All endpoints healthy."
              exit 0
            fi
            sleep 5
          done
          echo "ERROR: One or more health checks failed at https://$DOMAIN" >&2
          exit 1
